import { NextResponse, NextRequest } from "next/server"; import { securityHeaders } from "@/lib/security/headers"; export function middleware(req: NextRequest) { const res = NextResponse.next(); const hdrs = securityHeaders(); Object.entries(hdrs).forEach(([k,v]) => res.headers.set(k, v)); return res; } export const config = { matcher: ["/((?!_next/static|_next/image|favicon.ico|public).*)"] };
