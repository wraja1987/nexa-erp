/** local-safe config */
const config = {
  reactStrictMode: true,
  eslint: { ignoreDuringBuilds: true },
  typescript: { ignoreBuildErrors: true },
  experimental: {},
  webpack(cfg){ return cfg; }
};
module.exports = config;
