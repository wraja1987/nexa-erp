Import `ops/dashboards/nexa-slo-overview.json` into Grafana.

Steps:
- Login as admin (GF_SECURITY_ADMIN_USER/PASSWORD).
- Import the JSON via Dashboards > Import.
- Create a read-only user and share dashboard link.
- Use Dashboard > Snapshot for external sharing.

