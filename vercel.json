{
  "version": 2,
  "installCommand": "corepack enable && corepack prepare pnpm@10 --activate && pnpm install --frozen-lockfile || (npx -y npm@9 install --package-lock-only --legacy-peer-deps && npm ci --legacy-peer-deps)",
  "buildCommand": "cd apps/web && pnpm build || (cd apps/web && npm run build)",
  "outputDirectory": "apps/web/.next"
}



