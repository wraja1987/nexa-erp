# Operations & Monitoring

Sentry (client+server); Prometheus + Alertmanager → Gmail; Grafana dashboards (infra/latency/errors/DB/SLO);
external uptime; synthetic login→dashboard KPI; basic chaos tests.
