// Artillery processor for load test
// Adds custom logic if needed (e.g., authentication tokens, dynamic URLs)

module.exports = {
  // Add any custom processor functions here if needed
  // For now, we're using simple GET requests without auth
};

