# Configuration & Environment

Required variables (examples): DATABASE_URL, REDIS_URL, SESSION_SECRET,
STRIPE_API_KEY, TRUELAYER_CLIENT_ID/SECRET, HMRC_MTD_*.

Secrets must be stored in a secrets manager. Feature flags are in `configuration/feature-flags.md`.
