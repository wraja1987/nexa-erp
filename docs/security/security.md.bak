# Security Model

- RBAC with least privilege; SoD checks on sensitive actions.
- MFA for privileged roles; device/session policies; password rules.
- CSRF, XSS and click‑jacking protections; secure headers; secrets handling.
- Audit trail for sensitive events; evidence retention.
